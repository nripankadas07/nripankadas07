[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nripankadas07-profile"
version = "0.1.0"
description = "Public GitHub profile README, portfolio audit, and link-check tooling."
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [{name = "Dr. Nripanka Das"}]

[project.scripts]
profile-check-links = "scripts.check_links:main"
